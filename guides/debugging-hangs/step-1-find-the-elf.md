# Step 1: Find the ELF


Hanging tests show in build output like:

```
oldest: .../work/sail-rv64-max/build/rv64i/Vf16/Vf16-vfmv.s.f.sig
```

ELF at that path + `.elf`. Partial log at that path + `.log`.
