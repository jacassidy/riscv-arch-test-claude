# Coverpoint Template Reference


Coverpoint templates live in main repo at `generators/coverage/src/covergroupgen/templates/` (scalar) and `…/templates/vector/`. Guide cover file naming, format rules, copy-paste patterns, `ins` object API. Single source of truth — no duplicate elsewhere.
