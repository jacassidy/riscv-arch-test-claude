# File Naming


- **One coverpoint per file** — every template file has exactly one coverpoint
- **Filename**: matches coverpoint name exact (e.g., `cp_custom_vfp_flags_set.sv`, `cp_vstart_gt_vl.sv`)
- Each file independent — define helpers inline
