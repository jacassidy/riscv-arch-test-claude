# Transition Bins (NV1/DZ1/NX1)


Framework clears fflags before each test. Cover "stays set" (`FLAG1`) → generate flag-triggering test **twice in a row** so sample[i] and sample[i+1] both have flag set.
