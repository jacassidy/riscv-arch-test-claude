# Custom Test Generation Scripts Guide


> **This file lives in `riscv-arch-test-claude`.** All new Claude-generated files belong here, not in main repo. Actual `cp_custom_*.py` scripts live in `(main repo) generators/testgen/scripts/custom/`.
