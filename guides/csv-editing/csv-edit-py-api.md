# CSV Editing

`tools/csv_edit.py` does NOT exist. Use Read/Edit tools directly on CSV files.

CSV paths: `working-testplans/csvs/Vector - V{ls,x,f}_custom_definitions.csv`

Workflow: Read tool → inspect headers/rows → Edit tool for targeted cell changes.
