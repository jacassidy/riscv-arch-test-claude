# Normative Rules → Coverpoint Mapping


Generates `coverpoints/norm/Vx.yaml` (and siblings) mapping RISC-V V spec normative rules to functional coverpoints.

> **TL;DR** Edit CSV, re-run fill script. CSV = database; YAML = generated artifact. `tools/fill_vx_coverpoints.py` (THIS repo) own YAML — NOT main-repo `generators/ctp/norm_yaml_gen.py`.
