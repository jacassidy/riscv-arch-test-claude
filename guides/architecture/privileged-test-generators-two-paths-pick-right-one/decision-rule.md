## Decision rule


- Many instructions × few coverpoints → Path A.
- Few instructions × many CSR / mode permutations → Path B.
- Mixed → Path B for scenario glue; call into shared helpers (see `priv/extensions/ExceptionsCommon.py` for examples).
