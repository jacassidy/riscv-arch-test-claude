# architecture.md — Project Architecture & Reference


> **This file lives in `riscv-arch-test-claude`.** All new Claude-generated files belong here, not in main repo. Main repo = current working directory (whichever checkout or worktree this CLAUDE.md symlinked into).
